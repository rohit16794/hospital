How to run the application:

1. Clone the repository.
2. Change the directory to the application directory, run "sudo npm install". This will install all the packages required.
3. In the terminal run node server.js.
4. In the browser, go to localhost:3000.
